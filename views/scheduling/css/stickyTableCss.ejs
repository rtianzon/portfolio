<style>

.table-container {
  overflow-x: auto;
  margin-top: 4rem;
  position: relative;
}

table {
  border-collapse: collapse;
  width: 100%;
}

th,
td {
  padding: 5px;
  border: 1px solid #ddd;
  font-weight: 500;
  font-size: small;
  min-width: 10rem;
  text-align: center;
}

th.sticky-column,
td.sticky-column {
  position: sticky;
  left: 0;
  background-color: #f5f5f5;
  z-index: 2;
}

th.sticky-column:before,
td.sticky-column:before {
  content: "";
  position: absolute;
  top: 0;
  left: -1px;
  height: 100%;
  width: 1px;
  background-color: white; /* Change the border color here */
  z-index: 1;
}

th.sticky-column {
  z-index: 3;
}

/* tr:first-child th {
  border-top: none;
} */

.booked {
  background: var(--red-color-main);
  color: white;
  font-weight: 600;
  padding: 0;
  margin-bottom: 0.5rem;
  border-radius: 10px;
}

.booked i {
  background: var(--red-color-sub);
  width: 100%;
  bottom: 100;
  cursor: pointer;
  border-radius: 10px;
}

.vacant {
  background: var(--green-color-main);
  color: white;
  font-weight: 600;
  padding: 0;
  margin-bottom: 0.5rem;
  border-radius: 10px;
}

.vacant i {
  background: var(--green-color-sub);
  width: 100%;
  bottom: 100;
  cursor: pointer;
  border-radius: 10px;
}

.vacantOptions {
  position: absolute;
  width: 200px;
  height: 100px;
  border: none;
  background: var(--green-color-main);
  z-index: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-top: -1rem;
  border-radius: 10px;
}

.vacantOptions span {
  color: white;
  background: var(--green-color-sub);
  padding: 10px;
  border-radius: 10px;
  width: 5rem;
  cursor: pointer;
}

/* Styling the scrollbar */
.table-container::-webkit-scrollbar {
  width: 10px; /* Width of the scrollbar */
}

.table-container::-webkit-scrollbar-track {
  background: #f1f1f1; /* Color of the track */
}

.table-container::-webkit-scrollbar-thumb {
  background: #888; /* Color of the scrollbar thumb */
}

.table-container::-webkit-scrollbar-thumb:hover {
  background: #555; /* Color of the scrollbar thumb on hover */
}

</style>